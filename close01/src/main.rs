use std::thread;
use std::time::Duration;

fn main() {
    println!("Hello, world!");
}

fn workout(tensity: u32, num: u32) {
    let exp = |n|{
        println!("缓慢的计算");
thread::sleep(Duration::from_secs(2));
n
    };
}
